import React from 'react';

import './graphics.scss';

/* eslint-disable-next-line */
export interface GraphicsProps {}

export const Graphics = (props: GraphicsProps) => {
  return (
    <div>
      <h1>Welcome to graphics component!</h1>
    </div>
  );
};

export default Graphics;
